apiVersion: v1
kind: Service
metadata:
  name: anfisa-backend
  labels:
    app: {{ .Values.name }}
spec:
  ports:
  - name: anfisa-backend
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: {{ .Values.name }}
  sessionAffinity: None
  type: ClusterIP